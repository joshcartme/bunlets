# some bun tools

## gitpr

Add something like this to your profile:
```
alias gitpr="bun ~/path/to/gitpr.ts"
```

Then within a git repository you can do:

```
gitpr
```

Which will give output like:

```
https://github.com/joshcartme/squiddly/compare/main...whatever-branch-you-were-on
```
